{
  "name": "tessel-board", "version": "0.0.3",

  "main": "index.js",

  "scripts": {
    "start": "npm install && bower install",
    "start": "tessel run index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "connect": "latest"
  },

  "devDependencies": {
    "connect-static": "latest"
  },

  "license": "GPL"
}
